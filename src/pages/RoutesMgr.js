import React from "react";
import {BrowserRouter as Router, Routes, Route} from 'react-router-dom';

// PrivateRoute: is needed to be logined
function PrivateRoute() {
    
}

const RouteMgr = () => {
    return(
        <Routes>

        </Routes>
    );
}